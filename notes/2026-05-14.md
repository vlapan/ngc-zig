
## ETag Desync Fix
Removed `test/*.etag` from `.gitignore` and began tracking them in git alongside the `.csv` files.
**Reason:** If the CSV data is tracked in git, its ETag metadata *must* also be tracked. Previously, doing a `git checkout HEAD -- test/*.csv` would revert the data to the past, but the untracked `.etag` file remained in the future. The `fetch-data.sh` script would send the "future" ETag, receive a HTTP 304 Not Modified, and silently fail to download the data needed to fix the desynchronized CSV. By tracking both, a `git restore test/` or a branch checkout guarantees the ETag matches the exact bytes of the CSV file currently on disk.

## State Management Refactor
Moved all planned tasks, optimizations, and future feature backlogs from static daily notes (like `2026-05-13.md`) into a single mutable state file: `knowledge/tasks.md`.
**Reason:** Daily notes are append-only narratives, meaning future items trapped inside them get "forgotten" in later sessions. `knowledge/tasks.md` now acts as the single source of truth for the project roadmap, providing O(1) discovery at the start of every session without needing to `grep` through history. `KNOWLEDGE.md` has been updated to enforce this new workflow rule.

## Benchmark: Branchless IPv6 Formatting
Implemented the `@clz` hardware zero-counting optimization for the IPv6 text formatter.
- **Goal:** Replace the `inline for` and data-dependent `if (nib != 0 or started)` boolean branch with a deterministic calculation of characters to print.
- **Results:**
  - Instructions retired increased very slightly: `3.896B` -> `3.898B` (+1.8M).
  - CPU cycles elapsed remained identical within the noise floor (~`1.01B`).
  - Max RSS memory remained locked at `46.9MB`.
  - The runtime hovered precisely at `~339ms`.
- **Conclusion:** While conceptually cleaner and mathematically deterministic, it did not yield the massive savings seen in IPv4 because the original boolean flag was already being compiled into highly efficient predicated `CMOV` instructions by LLVM, and bitwise shifts are already 1-cycle operations. The `@clz` approach is committed as it removes data-dependencies from the pipeline.

## Benchmark: Lightweight Index Sorting (Pointer Sorting)
Implemented the `sortIndicesBySizeDesc` optimization.
- **Goal:** Replace `std.mem.sort` on a massive array of 64-byte `IPv6Range` structs with sorting an array of 4-byte `u32` integers, drastically reducing memory bandwidth overhead during physical element swapping.
- **Results:**
  - **Instructions retired PLUMMETED:** `3.89B` -> `2.62B` (A massive reduction of ~1.27 Billion instructions!).
  - **CPU cycles elapsed dropped:** `1.00B` -> `812M` (Saved ~200 Million CPU cycles).
  - **Runtime dropped:** `~330ms` -> `~260ms` (~21% faster execution).
  - **Memory Impact:** Max RSS increased slightly from `44.8 MB` to `46.1 MB` (+1.3MB) because we are now allocating two extra `[]u32` arrays (one for IPv4, one for IPv6) to hold the indices alongside the original struct arrays.
- **Conclusion:** A massive success. Trading 1.3MB of RAM to instantly bypass 1.2 Billion CPU instructions is an exceptional optimization trade-off. The execution is significantly faster, and the automated `make bench` output proved the mathematical CIDR output remained perfectly intact.

## Benchmark: Branchless Trie Node Allocation (appendAssumeCapacity)
Replaced the standard `try self.nodes.append` with an explicit bounds check evaluating against `.capacity` followed by `.appendAssumeCapacity()`.
- **Goal:** Strip branching checks inside the `allocNode` loop, which executes over 1.5 million times during `insertRange`.
- **Results:**
  - **Instructions retired:** `2.62B` -> `2.52B` (Saved exactly **~100 Million instructions**).
  - **Cycles elapsed:** Minor variations but generally stabilized around `809M` on an un-contended Hot run.
  - **Runtime & Memory:** RSS remains exactly `46.1 MB`. The execution time stabilized at `0.27s`. 
- **Conclusion:** As theorized, bypassing the internal zig `ArrayList.append` branch explicitly drops exactly ~100M instructions. While the cycle drop is small, the instruction footprint reduction is deterministic and guaranteed. Output mathematically matches the baseline perfectly.

## Benchmark: Algorithmic Pre-Flattening
Repurposed the sweep-line 1D algorithm to completely flatten the dataset *before* Trie insertion.
- **Goal:** Resolve all overlapping datasets into a mathematical sequence of perfectly disjoint ranges. The Trie is now purely a CIDR boundary fragmenter rather than an overlap resolver.
- **Implementation:** Sweep line across a unified array of `Event(T)` values (starts and ends). Tracks active IDs, handles overlaps by extracting the range with the highest priority (smallest size, smallest end, smallest country). Contiguous winning segments of the identical country are natively merged in a single pass.
- **Results:**
  - **Instructions retired:** `2.50B` -> `2.31B` (Saved **~186 Million instructions**).
  - **Cycles elapsed:** `679M` -> `628M` (Saved **~50 Million cycles**).
  - **Runtime:** `~0.26s` -> `~0.21s`.
  - **Trie Overrides:** `2`. The only overrides remaining in the entire system are from `static_v4_ranges` (the intentional hole-punch file). The core dataset now exhibits strictly zero overrides.
- **Conclusion:** Huge success. The dataset is fully mathematically sanitized before it touches the Trie, completely bypassing Radix Node fragmentation.

## Internal Telemetry Fix
Updated the timestamp retrieval logic in `src/main.zig`. Replaced `std.Io.Timestamp.now(..., .real)` with `std.Io.Timestamp.now(..., .awake)` (equivalent to monotonic uptime on POSIX systems) to guarantee monotonically increasing nanosecond measurements that are immune to NTP drift during benchmarking.

## Memory Impact of Pre-Flattening (RSS Increase)
During a detailed review of the Algorithmic Pre-Flattening optimization, it was noted that while CPU instructions dropped massively, the Max RSS memory footprint increased from ~46.1 MB to ~79.6 MB (+33.5 MB).
**Why this happened:** The `flatten` algorithm requires multiple intermediate array allocations that exist simultaneously in memory before they are cleaned up:
1. The raw `ipv4_ranges` / `ipv6_ranges` parsed directly from the CSV.
2. The `events` array (holds 2 events per input range, totaling ~660k structs for IPv4).
3. The `flattened_v4` / `flattened_v6` output arrays.
**The Trade-off:** We intentionally traded ~33.5 MB of temporary RAM to strip ~186 Million CPU instructions and eliminate complex fragmentation logic from the Radix Trie. For a CI/CD background job, an 80MB peak RAM footprint is well within safe bounds.
**Future Mitigation:** If memory constraints become an issue, we can refactor `flatten` to take a pointer to the `IpTrie` and stream the disjoint segments directly into the tree as they are calculated, completely eliminating the need for the `flattened_vX` intermediate arrays. This has been added to the backlog.

## Future Telemetry Enhancements Identified
During architectural review, three new telemetry metrics were identified as highly valuable for future implementation. Adding these to the backlog:

### 1. Radix Trie Node Count
- **Metric format:** `Trie Nodes Allocated: 1,402,392`
- **Why it matters:** It tells us how fragmented the IP space is and gives an exact representation of memory usage during Phase 2. This would act as a great baseline to monitor if we implement further optimizations to prevent node splitting.

### 2. Contiguous Merges (Sweep Line Efficiency)
- **Metric format:** `Contiguous Blocks Merged: 41,202`
- **Why it matters:** This tracks how "dirty" the incoming datasets are (e.g. adjacent `/24` blocks in the GeoFeed that are both assigned to 'US') and tells us exactly how much work the sweep-line algorithm is saving the Trie from having to do.

### 3. File I/O vs Processing Time Split
- **Metric format:** `I/O Parsing Time: 80ms`, `Sweep Line Time: 40ms`, `Radix Time: 100ms`
- **Why it matters:** As we optimize further, knowing exactly which phase is the bottleneck (Parsing strings vs sweeping vs tree traversal) is crucial for knowing where to focus our effort next.

## Telemetry Enhancements Implemented
Implemented the three highly granular telemetry enhancements requested by the operator to better profile the new two-phase architecture.

**Findings & Metrics Revealed:**
1. **Radix Trie Node Count**: Revealed exactly `1,020,898` nodes for IPv4 and `1,595,702` nodes for IPv6. This gives us a concrete baseline for tracking IP space fragmentation during CIDR generation.
2. **Contiguous Merges**: Showed only `157` (IPv4) and `43` (IPv6) merges. This proves the upstream raw CSV datasets are surprisingly clean with very few adjacent fragmented blocks of the identical country.
3. **Pipeline Profiling (Timings)**: Exposed that the bottleneck is overwhelmingly Phase 2. `I/O & Parsing` takes ~51ms, `Phase 1 (Sweep Line)` takes only ~14ms, while `Phase 2 (Radix Trie CIDR fragmentation)` takes ~167ms.

**Holistic Benchmark Trade-offs:**
- **Max RSS:** Stable at 79.6 MB.
- **CPU Instructions:** Stable at ~2.31 Billion.
- **Binary Size Regression:** The binary size grew from **263K** to **280K** (+17KB). This is an expected trade-off due to pulling in 128-bit timestamp math and instantiating several new `std.debug.print` formatted string segments, but perfectly acceptable for the debug visibility it provides.
- Output mathematically unchanged.

## Memory Optimization: Streaming Flattened Output to Trie
Refactored the `flatten()` sweep-line algorithm to accept the `IpTrie` directly and stream resolved non-overlapping network segments directly into the Trie, eliminating the intermediate `flattened_v4` and `flattened_v6` dynamic arrays.

**Holistic Benchmark Impact:**
- **Max RSS:** Dropped drastically from **79.6 MB** to **61.3 MB** (-18.3 MB reduction!).
- **CPU Instructions:** Dropped slightly from ~2.310B to ~2.305B (-5M instructions) due to removing the intermediate array copy loops.
- **Pipeline Profiling:** `Phase 1 (Flatten)` timing now encapsulates both the Sweep-Line execution and Radix Trie node allocations (since they happen as a single streamed pass). E.g., `Phase 1` now reads ~136ms, while `Phase 2` (which is now strictly just `optimize` + `dump`) reads ~57ms.

Output remains mathematically perfect and unchanged.
