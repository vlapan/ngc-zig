# 2026-06-12

## Done
- Added doc comment to `src/scan.zig:5-8` explaining why callers needing two bytes should call findByte twice (single-pass was +63% slower).
- Fixed stale references across 5 knowledge files after module renames (swar→scan, parser→parse, function renames):
  - `commit-conventions.md`: scope list (swar→scan), examples (parser→parse, fastParseInt→parseInt, swarm→scan)
  - `architecture.md`: parser.zig→parse.zig, swar.zig→scan.zig, appendStaticFile→staticFile
  - `flow-review.md`: fastParseInt→parseInt, parseCsvLine→csvLine, swar.findByte→scan.findByte, file/function table
  - `tasks.md`: 7 stale file/function references in active tasks
  - `testing-framework.md`: full directory structure, code examples, workflow rules, and adding-new-test section updated from old test/ layout to current spec/ layout
  - All Updated: dates bumped to 2026-06-12
- `make check` passes.
