worker_processes 1;
daemon off;
pid nginx-baseline.pid;
error_log error-baseline.log;

events {
    worker_connections 16;
}

http {
    server {
        listen 8888;
        server_name localhost;
        location / {
            return 200 "ok";
        }
    }
}
